# S1 r1 执行审计补充（不是修改冻结协议）

记录时间：2026-09-26，首个任务完成16个候选之后；本批没有读取任何测试评分。

1. PR #4 的方案建议填写总 token/总时间限制。本批协议实际只冻结32提案、每请求16384/8192输出上限及180秒网络超时；没有实现总token/时间硬截断。后续结果只能称同提案预算的诊断筛查，不称同token或等总墙钟实验。不能在已运行后补一个上限再宣称事先冻结。
2. `study.verify()` 在启动时读取search/test原始字节核对SHA256；search evaluator只得到probe/validation快照、候选语言无I/O、模型不收到test数据和测试评分。但“搜索进程未打开过test文件”不是事实；可以声称数据流/评分隔离，不能声称文件访问权限隔离。下一版应将test哈希校验放在独立预检/测试阶段，给在线入口加打开禁止测试。
3. 当前verify检查工具源码而未显式比较manifest.source与当前source_record；本轮前后将独立核验全部依赖源码哈希、保留结果。不得仅凭verify返回就宣称全依赖已验证。运行期间不修改被导入的控制器、提示、评价器或运行器。
4. r1是旧F/R开发池规则可观测性，不是PR #4的SP/WR/FB/TS/AD五策略实现；标签不同、多B条目不等价于真实外层模式。后续独立审计区分谱系、结构签名和probe行为；这些分析不参与本轮选择。
5. GitHub在r2结果之后多次连接失败。所有协议先在本地commit、manifest记录时间；只有能核实远端已接收的commit才称调用前公开冻结。S0 r2的manifest已在调用前推送，r3和S1到此仅本地冻结，不冒称远端已推送。

上述偏离不触发删除运行或改动对照，全部失败/无竞争运行保留。它们缩小解释边界，需要在新协议中修复，而不是在进行中的同一实验悄悄替换。
