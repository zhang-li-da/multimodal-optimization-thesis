# 并行工程准备：非 S1 模型效果结果

下列文件不被正在执行的冻结 S1 导入，不改变 S1 对照或数据。

- `policies.py`：SP/WR/FB/TS/AD 五种外层探索—开发策略的纯状态机草案；只有合成事件测试，未开展真实五策略搜索。没有资格报告 AD 的性能优势。
- `directions.py`：按特征、算子和条件结构生成的粗粒度确定性签名。局部变量改名不建立新方向；分别记录谱系和行为哈希。它不是语义等价类或真实吸引域，也尚未由独立评阅者校准。
- `tsp_scale.py`：独立 n=50/100/200 受限 `priority(f)` 适配器。向量化计算特征，候选函数仍在安全解释器中执行；不接任意 Python 工具/代码。

## 无 API profiling（2026-09-26）

9个全新工程实例：50/100/200 × uniform/clustered/grid；3手写种子规则 × 24或10n局部检查，共54次程序评价。无模型调用。参考为四起点nearest-neighbor + 最多200n次2-opt delta checks，各参考路线独立验证长度，**不是最优解证书**。有符号相对参考差距保留负值。

| 城市数 | 单程序单实例中位秒 | 最大秒 | 参考准备均值秒 |
|---|---:|---:|---:|
|50|0.00663|0.02719|0.00986|
|100|0.02510|0.02645|0.03321|
|200|0.10418|0.10724|0.08152|

本机正在串行模型搜索，profiling不是专用空载硬件基准。它支持大一点的TSP适配器工程可行，不证明搜索方法跨规模有效。若使用它进行后续正式实验，需单独冻结数据、算子预算、接口提示、控制器、同总资源限制和测试文件权限。不得混用旧Held–Karp接口给50/100城市求精确最优。

## 仍待解决的设计细节

五策略草案目前用Bernoulli(0.5)表示FB的期望1:1，并非严格交替配额；后续必须冻结哪种定义。容量保护及方向别名是启发式，不是对所有表面修改的语义证明。保护关闭开关仅影响当前草案中的容量锁定，不是完整的S3“无保证次数”因子，不能据它宣称已实现S3归因。尚需完整的预算账本、未决/超时处理、fixture端到端测试以及方向盲审，才能注册S2主实验。
